# Generated Capacitor/Android project — rebuilt by build-apk.sh
android-build/

# Build output
*.apk
*.aab

# Signing material — never commit these
*.keystore
*.jks
keystore.properties

node_modules/

# Machine-specific overrides (JDK/SDK paths) — never committed
build.local.conf
